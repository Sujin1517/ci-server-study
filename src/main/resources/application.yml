server:
  port: 8080
spring:
  application:
    name: test_back
  datasource:
    url: jdbc:mysql://localhost:3306/test_board?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    password: 1234
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true